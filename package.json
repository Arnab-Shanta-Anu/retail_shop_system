{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "start": "concurrently \"php artisan serve\" \"npm --prefix ./react-frontend run dev\""
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "concurrently": "^8.2.0",
        "laravel-vite-plugin": "^0.7.5",
        "vite": "^4.0.0"
    }
}
